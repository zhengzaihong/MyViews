<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dz.solc.myviews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:replace="android:appComponentFactory"
        android:appComponentFactory="whateverString"
        android:name=".App"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.TimeViewActivity"></activity>
        <activity android:name=".ui.MarqueeViewActivity"></activity>
        <activity android:name=".ui.MenueViewActivity"></activity>
        <activity android:name=".ui.TipDialogActivity"></activity>
        <activity android:name=".ui.ImageViewActivity"></activity>
        <activity android:name=".ui.CalendarViewActivity"></activity>
        <activity android:name=".ui.TableViewActivity" />
    </application>

</manifest>